## To run the project

In the project directory, install the dependencies:

### `npm install`

Build the project:

### `npm run build`

Install node server globally

### `npm install -g serve`


Run the server: 

### `serve -s build`